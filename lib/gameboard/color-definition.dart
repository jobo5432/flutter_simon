import 'package:flutter/cupertino.dart';

class ColorDefinition {
  final Color color;
  final int note;
  final Color highlightColor;
  final int offset;

  ColorDefinition(this.color, this.offset, this.highlightColor, this.note);
}
